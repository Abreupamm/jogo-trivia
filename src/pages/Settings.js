import React from 'react';

import {
  Header,
} from '../components';

const Settings = () => (
  <>
    <Header />
    <div id="settings-page">
      <h1 data-testid="settings-title">
        Configurações
      </h1>
    </div>
  </>
);

export default Settings;
